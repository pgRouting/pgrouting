#!/usr/bin/perl -w

%main::tests = (
    'any' => {
        'comment' => 'Recipes test for any versions.',
        'data' => [],
        'tests' => [qw(
            parallel_handling
            example_recipe
            )]
    },

);

1;

BEGIN;
BEGIN
SET client_min_messages TO NOTICE;
SET
SELECT * FROM pgr_vrpOneDepot(
    'SELECT * FROM vrp_orders',
    'SELECT * FROM vrp_vehicles',
    'SELECT * FROM vrp_distance',
    1);
ERROR:  All vehicles must depart & arrive to same node
CONTEXT:  PL/pgSQL function _pgr_vrponedepot(text,text,text,integer) line 46 at RETURN QUERY
SQL function "pgr_vrponedepot" statement 1
ROLLBACK;
ROLLBACK

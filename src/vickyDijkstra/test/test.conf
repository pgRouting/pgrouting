#!/usr/bin/perl -w

%main::tests = (
    'any' => {
        'comment' => 'Dijkstra test for any versions.',
        'data' => [ ],
        'tests' => [qw(
            doc-vickyDijkstra
            )],
        'documentation' => [qw(
            doc-vickyDijkstra
            )]
    },

);

1;

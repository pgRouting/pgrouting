#!/usr/bin/perl -w

%main::tests = (
    'any' => {
        'comment' => 'Dijkstra test for any versions.',
        'data' => [ ],
        'tests' => [qw(
            custom
            )],
        'documentation' => [qw(
            doc-dijkstraTRSP
            )]
    },

);

1;

#!/usr/bin/perl -w

%main::tests = (
    'any' => {
        'comment' => 'Dijkstra test for any versions.',
        'data' => [ ],
        'tests' => [qw(
            )],
        'documentation' => [qw(
            )],
        'not testing' => [qw(
            doc-dijkstraTRSP
            pgtap_design
            custom
            )]
    },

);

1;

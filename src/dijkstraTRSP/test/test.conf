#!/usr/bin/perl -w

%main::tests = (
    'any' => {
        'comment' => 'Dijkstra test for any versions.',
        'data' => [ ],
        'tests' => [qw(
            doc-dijkstraTRSP
            two_edge_test
            )],
        'documentation' => [qw(
            doc-dijkstraTRSP
            two_edge_test
            )]
    },

);

1;

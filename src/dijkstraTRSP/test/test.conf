#!/usr/bin/perl -w

%main::tests = (
    'any' => {
        'comment' => 'Dijkstra test for any versions.',
        'data' => [ ],
        'tests' => [qw(
            doc-dijkstraTRSP
            pgtap_design
            )],
        'documentation' => [qw(
            doc-dijkstraTRSP
            )]
    },

);

1;

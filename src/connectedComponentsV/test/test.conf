#!/usr/bin/perl -w

%main::tests = (
    'any' => {
        'comment' => 'Dijkstra test for any versions.',
        'data' => [ ],
        'tests' => [ ],
        'pending_tests' => [qw(
            doc-connectedComponentsV
            )],
        'documentation' => [ ]
    },

);

1;

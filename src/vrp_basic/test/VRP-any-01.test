select (r).order_id, (r).order_pos, (r).vehicle_id, (r).cost from ( select pgr_vrpOneDepot('select * from vrp_orders'::text, 'select * from vrp_vehicles'::text, 'select * from vrp_distance'::text, 1) as r ) as foo;



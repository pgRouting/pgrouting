#!/usr/bin/perl -w

%main::tests = (
    'any' => {
        'comment' => 'VRP Single depot test for any versions.',
        'data' => ['../../../toots/testers/vrpOneDepot.data'],
        'tests' => [qw(
            oneDepotWrapper
            doc-pgr_vrpOneDepot
            )],
        'documentation' => [qw(
            oneDepotWrapper
            doc-pgr_vrpOneDepot
            )]
        },
);

1;

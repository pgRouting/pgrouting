#!/usr/bin/perl -w

%main::tests = (
    'any' => {
        'comment' => 'VRP Single depot test for any versions.',
        'data' => ['VRP-any-00.data'],
        'tests' => [qw(
            oneDepotWrapper
            doc-pgr_vrpOneDepot
            )],
        'documentation' => [qw(
            oneDepotWrapper
            doc-pgr_vrpOneDepot
            )]
        },
);

1;

#!/usr/bin/perl -w

%main::tests = (
    'any' => {
        'comment' => 'Components test for any versions.',
        'data' => [ ],
        'tests' => [qw(
            doc-pgr_connectedComponentsV
            doc-pgr_strongComponentsV
            )],
        'documentation' => [qw(
            doc-pgr_connectedComponentsV
            doc-pgr_strongComponentsV
            )]
    },

);

1;

#!/usr/bin/perl -w

%main::tests = (
    'any' => {
        'comment' => 'pgr_pdDijkstra (new signature) old large test',
        'data' => ["large.data" ],
        'tests' => [qw(
            bdDijkstra-large
            doc-bdDijkstra
            )],
        'documentation' => [qw(
            doc-bdDijkstra
            )]
    },
);

1;

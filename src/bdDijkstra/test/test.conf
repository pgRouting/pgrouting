#!/usr/bin/perl -w

%main::tests = (
    'any' => {
        'comment' => 'Dijkstra test for any versions.',
        'data' => ["large.data" ],
        'tests' => [qw(
            bdDijkstra-large
            doc-bdDijkstra
            )],
        'documentation' => [qw(
            doc-bdDijkstra
            )]
    },

);

1;

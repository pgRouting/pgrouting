#!/usr/bin/perl -w

%main::tests = (
    'any' => {
        'comment' => 'pgr_pdDijkstra (new signature) old large test',
        'data' => ["large.data" ],
        'tests' => [qw(
            doc-bdDijkstra
            )],
        'nottested' => [qw(
            bdDijkstra-large
            doc-bdDijkstra
            )]
    },
);

1;

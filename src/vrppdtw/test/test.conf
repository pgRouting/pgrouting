#!/usr/bin/perl -w

%main::tests = (
    'any' => {
        'comment' => 'Function test for any versions.',
        'data' => ['../../../tools/testers/vrppdtw_data.sql'],
        'tests' => [qw(
            )],
        'windows' => [qw(
            doc-gsoc_vrppdtw-cygwin
            pdp-any-02-cygwin
            )],
        'linux' => [qw(
            jet_customers
            doc-gsoc_vrppdtw
            pdp-any-02
            )],
        'documentation' => [qw(
            doc-gsoc_vrppdtw
            )],

        'nottesting' => [qw(
            )]
    },
);

1;

#!/usr/bin/perl -w

%main::tests = (
    'any' => {
        'comment' => 'Function test for any versions.',
        'data' => ['../../pickDeliver/test/pickDeliver.data'],
        'tests' => [qw(
            )],
        'cygwin' => [qw(
            doc-gsoc_vrppdtw-cygwin
            pdp-any-02-cygwin
            )],
        'linux' => [qw(
            doc-gsoc_vrppdtw
            pdp-any-02
            )],
        'documentation' => [qw(
            doc-gsoc_vrppdtw
            )],

        'nottesting' => [qw(
            )]
    },
);

1;

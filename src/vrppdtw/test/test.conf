#!/usr/bin/perl -w

%main::tests = (
    'any' => {
        'comment' => 'Function test for any versions.',
        'data' => ['../../../tools/testers/vrppdtw_data.sql'],
        'tests' => [qw(
            )],
        'windowss' => [qw(
            doc-gsoc_vrppdtw-cygwin
            pdp-any-02-cygwin
            )],
        'linux' => [qw(
            doc-gsoc_vrppdtw
            )],
        'documentation' => [qw(
            doc-gsoc_vrppdtw
            )],

        'nottesting' => [qw(
            pd_jet_customers
            jet_customers
            pdp-any-02
            )]
    },
);

1;

BEGIN;
BEGIN
SET client_min_messages TO NOTICE;
SET
--q1
SELECT * FROM pgr_gsoc_vrppdtw(
    'SELECT * FROM customer ORDER BY id',
    25, 200);
Cancel request sent
Cancel request sent
ERROR:  canceling statement due to user request
CONTEXT:  PL/pgSQL function _pgr_pickdeliver(text,integer,double precision,double precision,integer) line 39 at RETURN QUERY
SQL function "pgr_gsoc_vrppdtw" statement 1

DATA:::
According to the documentation given in the website it is noted that the data sets are in the following order.

               ***********************************************************************

Number of Vehicles  -- Capacity 

CustNo: -- X --  Y -- Demand -- Earliest Pick/Del Time -- Latest Pick/Del Time -- Service Time -- PickUp(Index to sib) -- Deliver(Index to sib)



              *************************************************************************


              
Handling data :

NumberofVehicles:   VehicleCount (int)
Capacity :          Capacity(int)
Speed :             speed(int)

Structs required to handle customers::   

struct cust{
        int nid;
        float X;
        float Y;
        int demand;
        int Earliest_TIME;
        int Delivery_TIME;
        int Service_TIME;
        int Pickup;
        int delivery;
};



struct order{
        int oid;
        float Xp;
        float Yp;
        float Xd;
        float Yd;
        int demand;
        int dis_p;
        int dis_d;
        //Handle Time Windows
        int assigned;
};


Sort all the orders based on dis_p;



Algorithm to handle this things:


Sequential Construction Algorithm and Hill Climbing Method

*********************************************
Write a class for each route 

class Route{
        Route();
        ~Route();

        public:
        int twv;
        int cv;
        int dist;
        int Path[100];
        int Orders[100];
        int path_length;

        int CostCal();
        int update(); // Calculate all the three variables.

};






#!/usr/bin/perl -w

%main::tests = (
    'any' => {
        'comment' => 'pgr_pdAstar (new signature)',
        'data' => [],
        'tests' => [qw(
            doc-pgr_bdAstar
            )],
        'documentation' => [qw(
            doc-pgr_bdAstar
            )]
    },
);

1;

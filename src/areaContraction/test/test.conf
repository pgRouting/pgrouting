#!/usr/bin/perl -w

%main::tests = (
    'any' => {
        'comment' => 'areaContraction test for any versions.',
        'data' => [ ],
        'tests' => [],
        'documentation' => [qw(

            doc-areaContraction

            )]
    },

);

1;

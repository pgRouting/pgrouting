#!/usr/bin/perl -w

%main::tests = (
    'any' => {
        'comment' => 'areaContraction test for any versions.',
        'data' => [ ],
        'tests' => [qw(

            doc-pgr_areaContraction
            proof_of_concept1

            )],
        'documentation' => [qw(

            doc-pgr_areaContraction
            proof_of_concept1

            )]
    },

);

1;

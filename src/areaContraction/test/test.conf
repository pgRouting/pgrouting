#!/usr/bin/perl -w

%main::tests = (
    'any' => {
        'comment' => 'areaContraction test for any versions.',
        'data' => [ ],
        'tests' => [qw(

            doc-pgr_areaContraction

            )],
        'documentation' => [qw(

            doc-pgr_areaContraction

            )]
    },

);

1;

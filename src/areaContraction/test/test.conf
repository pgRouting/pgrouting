#!/usr/bin/perl -w

%main::tests = (
    'any' => {
        'comment' => 'Dijkstra test for any versions.',
        'data' => [ ],
        'tests' => [qw(
            doc-areaContraction
            )],
        'documentation' => [qw(
            doc-areaContraction
            )]
    },

);

1;

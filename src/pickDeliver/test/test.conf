#!/usr/bin/perl -w

%main::tests = (
    'any' => {
        'comment' => 'Function test for any versions.',
        'data' => ['pickDeliver.data'],
        'windows' => [qw(
            doc-pickDeliver-macos
            jet_customers-macos
            differentVechiles-macos
            )],
        'linux' => [qw(
            doc-pickDeliver
            jet_customers
            differentVechiles
            )],
        'documentation' => [qw(
            doc-pickDeliver
            )],

#put here the ones that you are not testing (just a place holder)
        'nottesting' => [qw(
            doc-pickDeliver
            jet_customers
            )]
    },
);

1;

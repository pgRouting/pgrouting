#!/usr/bin/perl -w

%main::tests = (
    'any' => {
        'comment' => 'Function test for any versions.',
        'data' => [ '../../../tools/testers/pickDeliver_pgtap_data.sql', '../../../tools/testers/vrppdtw_data.sql' ],
        'windows' => [qw(
            doc-pickDeliverEuclidean-cygwin
            jet_customers-cygwin
            differentVechiles-cygwin
            )],
        'linux' => [qw(
            doc-pickDeliverEuclidean
            pickDeliver
            jet_customers
            differentVechiles
            )],
        'documentation' => [qw(
            doc-pickDeliverEuclidean
            )],

#put here the ones that you are not testing (just a place holder)
        'nottesting' => [qw(
            doc-pickDeliverEuclidean
            jet_customers
            differentVechiles
            )]
    },
);

1;

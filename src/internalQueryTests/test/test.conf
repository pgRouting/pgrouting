#!/usr/bin/perl -w

%main::tests = (
    'any' => {
        'comment' => 'XY edges test',
        'data' => [ ],
        'debugtests' => [qw(
            XYedge
            testCHedges
            )],
        'tests' => [qw(
            )],
        'documentation' => [qw(
            )],
        'nottested' => [qw(
            )]
    },
);

1;

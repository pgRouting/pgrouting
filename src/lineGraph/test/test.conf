#!/usr/bin/perl -w

%main::tests = (
    'any' => {
        'comment' => 'Dijkstra test for any versions.',
        'data' => [ ],
        'tests' => [qw(
            doc-lineGraph
            )],
        'documentation' => [qw(
            doc-lineGraph
            )]
    },

);

1;

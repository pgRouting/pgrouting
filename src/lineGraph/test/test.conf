#!/usr/bin/perl -w

%main::tests = (
    'any' => {
        'comment' => 'LineGrpah test for any versions.',
        'data' => [ ],
        'tests' => [qw(
            doc-pgr_lineGraph
            )],
        'documentation' => [qw(
            doc-pgr_lineGraph
            )]
    },

);

1;

#!/usr/bin/perl -w

%main::tests = (
    'any' => {
        'comment' => 'Partitioned astar.',
        'data' => ['partition-test-01.data'],
        'tests' => [qw(partition-test-01)]
        },
#    'vpg-vpgis' => {}, # for version specific tests
#    '8-1' => {},       # for pg 8.x and postgis 1.x
#    '9.2-2.1' => {},   # for pg 9.2 and postgis 2.1
);

1;

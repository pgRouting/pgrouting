#!/usr/bin/perl -w

%main::tests = (
    'any' => {
        'comment' => 'Dijkstra test for any versions.',
        'data' => [ ],
        'tests' => [qw(
            from2to8
            )],
        'documentation' => [qw(
            doc-dijkstraTR
            )]
    },

);

1;

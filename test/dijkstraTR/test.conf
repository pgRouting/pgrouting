#!/usr/bin/perl -w

%main::tests = (
    'any' => {
        'comment' => 'Dijkstra test for any versions.',
        'data' => [ ],
        'tests' => [qw(
            from2to12-change
            )],
        'documentation' => [qw(
            doc-dijkstraTR
            from2to12
            dijkstraTR-compare-dijkstra
            )]
    },

);

1;

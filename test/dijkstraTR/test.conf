#!/usr/bin/perl -w

%main::tests = (
    'any' => {
        'comment' => 'Dijkstra test for any versions.',
        'data' => [ ],
        'tests' => [qw(
            from2to12
            )],
        'documentation' => [qw(
            doc-dijkstraTR
            )]
    },

);

1;

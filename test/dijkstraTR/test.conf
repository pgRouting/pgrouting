#!/usr/bin/perl -w

%main::tests = (
    'any' => {
        'comment' => 'Dijkstra test for any versions.',
        'data' => [ ],
        'tests' => [qw(
            doc-dijkstraTR
            )],
        'documentation' => [qw(
            doc-dijkstraTR
            )]
    },

);

1;

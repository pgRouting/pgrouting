#!/usr/bin/perl -w

%main::tests = (
    'any' => {
        'comment' => 'Dijkstra test for any versions.',
        'data' => [ ],
        'tests' => [qw(
            doc-prim
            )],
        'documentation' => [qw(
            doc-funnyDijkstra
            )]
    },

);

1;

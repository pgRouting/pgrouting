#!/usr/bin/perl -w

%main::tests = (
    'any' => {
        'comment' => 'Prim algorithm tets.',
        'data' => [ ],
        'tests' => [qw(
            doc-prim
            )],
        'documentation' => [qw(
            doc-prim
            )]
    },

);

1;

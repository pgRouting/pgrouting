#!/usr/bin/perl -w

%main::tests = (
    'any' => {
        'comment' => 'Minimum Cost Maximum Flow test for any versions.',
        'data' => [ ],
        'tests' => [qw(
            doc-pgr_maxFlowMinCost
            doc-pgr_maxFlowMinCost_Cost
            )],
        'documentation' => [qw(
            doc-pgr_maxFlowMinCost
            doc-pgr_maxFlowMinCost_Cost
            )]
    },

);

1;

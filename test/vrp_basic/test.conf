#!/usr/bin/perl -w

%main::tests = (
    'any' => {
        'comment' => 'VRP Single depot test for any versions.',
        'data' => ['../../tools/testers/vrpOneDepot.data'],
        'tests' => [qw(
            doc-pgr_vrpOneDepot
            )],
        'nottested' => [qw(
            oneDepotWrapper
            )],
        'documentation' => [qw(
            doc-pgr_vrpOneDepot
            )]
        },
);

1;

#!/usr/bin/perl -w

%main::tests = (
    'any' => {
        'comment' => 'Minimum-cost Flow test for any versions.',
        'data' => [ ],
        'tests' => [qw(
            doc-pgr_minCostMaxFlow
            doc-pgr_minCostMaxFlow_Cost
            )],
        'documentation' => [qw(
            doc-pgr_minCostMaxFlow
            doc-pgr_minCostMaxFlow_Cost
            )]
    },

);

1;

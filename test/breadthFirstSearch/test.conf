#!/usr/bin/perl -w

%main::tests = (
    'any' => {
        'comment' => 'Breadth First Search algorithm tests.',
        'data' => [ ],
        'tests' => [qw(
            doc-pgr_breadthFirstSearch
            )],
        'documentation' => [qw(
            doc-pgr_breadthFirstSearch
            )]
    },

);

1;

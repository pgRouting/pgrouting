#!/usr/bin/perl -w

%main::tests = (
    'any' => {
        'comment' => 'LineGrpah test for any versions.',
        'data' => [ ],
        'tests' => [qw(
            doc-pgr_turnPenaltyGraph
            pgtap_design_turnPenaltyGraph
            doc-pgr_lineGraph
            pgtap_design
            )],
        'documentation' => [qw(
            doc-pgr_turnPenaltyGraph
            doc-pgr_lineGraph
            )]
    },

);

1;

#!/usr/bin/perl -w

%main::tests = (
    'any' => {
        'comment' => 'LineGrpah test for any versions.',
        'data' => [ ],
        'tests' => [qw(
            pgtap_design
            )],
        'documentation' => [qw(
            doc-pgr_lineGraph
            )]
    },

);

1;

#!/usr/bin/perl -w

%main::tests = (
    'any' => {
        'comment' => 'LineGrpah test for any versions.',
        'data' => [ ],
        'tests' => [qw(
            doc-pgr_turnPenaltyGraph
            pgtap_design_turnPenaltyGraph
            doc-pgr_lineGraph
            pgtap_design
            dijkstra-1
            dijkstra-2
            dijkstra-3
            )],
        'documentation' => [qw(
            doc-pgr_turnPenaltyGraph
            doc-pgr_lineGraph
            )]
    },

);

1;

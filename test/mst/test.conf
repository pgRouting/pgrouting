#!/usr/bin/perl -w

%main::tests = (
    'any' => {
        'comment' => 'Prim algorithm tets.',
        'data' => [ ],
        'tests' => [qw(
            doc-prim
            doc-kruskal
            )],
        'documentation' => [qw(
            doc-prim
            doc-kruskal
            )]
    },

);

1;

#!/usr/bin/perl -w

%main::tests = (
    'any' => {
        'comment' => 'Prim algorithm tets.',
        'data' => [ ],
        'tests' => [qw(
            kruskal-flags
            )],
        'documentation' => [qw(
            doc-pgr_prim
            doc-pgr_kruskal
            doc-pgr_randomSpanTree
            )]
    },

);

1;

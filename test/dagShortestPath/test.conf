#!/usr/bin/perl -w

%main::tests = (
    'any' => {
        'comment' => 'Dijkstra test for any versions.',
        'data' => [ ],
        'tests' => [qw(
            doc-dagShortestPath
            )],
        'documentation' => [qw(
            doc-dagShortestPath
            )]
    },

);

1;

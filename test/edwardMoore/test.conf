#!/usr/bin/perl -w

%main::tests = (
    'any' => {
        'comment' => 'Edward Moore algorithm tests.',
        'data' => [ ],
        'tests' => [qw(
            doc-pgr_edwardMoore
            )],
        'documentation' => [qw(
            doc-pgr_edwardMoore
            )]
    },

);

1;

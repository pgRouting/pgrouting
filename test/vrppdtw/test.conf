#!/usr/bin/perl -w

%main::tests = (
    'any' => {
        'comment' => 'Function test for any versions.',
        'data' => ['../../tools/testers/vrppdtw_data.sql'],
        'tests' => [qw(
            )],
        'windows' => [qw(
            )],
        'linux' => [qw(
            doc-gsoc_vrppdtw
            )],
        'documentation' => [qw(
            doc-gsoc_vrppdtw
            )]
    },
);

1;

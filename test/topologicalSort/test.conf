#!/usr/bin/perl -w

%main::tests = (
    'any' => {
        'comment' => 'topologicalSort algorithm tets.',
        'data' => [ ],
        'tests' => [qw(
            doc-topologicalSort
            )],
        'documentation' => [qw(
            doc-topologicalSort
            )]
    },

);

1;

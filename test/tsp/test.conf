#!/usr/bin/perl -w

%main::tests = (
    'any' => {
        'comment' => 'TSP tests',
        'data' => [],
        'tests' => [qw(
            doc-pgr_TSPannealing
            )],
        'linux' => [qw(
            )],
        'macos' => [qw(
            )],
        # windows has a reversed path which is OK
        'windows' => [qw(
            win-pgr_TSPannealing
            )],
        'documentation' => [qw(
            doc-pgr_TSPannealing
            )]
    },
);

1;

#!/usr/bin/perl -w

%main::tests = (
    'any' => {
        'comment' => 'TSP tests',
        'data' => [],
        'tests' => [qw(
            doc-pgr_TSPannealingEuclidean
            )],
        'linux' => [qw(
            doc-pgr_TSPannealing
            )],
        'macos' => [qw(
            doc-pgr_TSPannealing
            )],
        # windows has a reversed path which is OK
        'windows' => [qw(
            win-pgr_TSPannealing
            )],
        'documentation' => [qw(
            doc-pgr_TSPannealing
            doc-pgr_TSPannealingEuclidean
            )]
    },
);

1;

#!/usr/bin/perl -w

%main::tests = (
    'any' => {
        'comment' => 'TurnPenaltyGraph for any versions.',
        'data' => [ ],
        'tests' => [qw(
            doc-pgr_turnPenaltyGraph
            pgtap_design
            dijkstra-1
            dijkstra-2
            dijkstra-3
            )],
        'documentation' => [qw(
            doc-pgr_turnPenaltyGraph
            )]
    },

);

1;

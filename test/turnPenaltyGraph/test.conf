#!/usr/bin/perl -w

%main::tests = (
    'any' => {
        'comment' => 'TurnPenaltyGraph for any versions.',
        'data' => [ ],
        'tests' => [qw(
            proofOfConcept1
            doc-pgr_turnPenaltyGraph
            )],
        'documentation' => [qw(
            doc-pgr_turnPenaltyGraph
            )]
    },

);

1;

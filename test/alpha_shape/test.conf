#!/usr/bin/perl -w

%main::tests = (
    'any' => {
        'comment' => 'Driving Distance test for any versions.',
        'data' => [],
        'tests' => [qw(
            doc-pgr_alphashape1.test.sql
            doc-pgr_alphashape.test.sql
            doc-pgr_pointsAsPolygon1.test.sql
            doc-pgr_pointsAsPolygon.test.sql
            specialCases.test.sql
            )],
        'documentation' => [qw(
            doc-pgr_alphashape
            )]
    },
    #    'vpg-vpgis' => {}, # for version specific tests
    #    '8-1' => {},       # for pg 8.x and postgis 1.x
    #    '9.2-2.1' => {},   # for pg 9.2 and postgis 2.1
);

1;

#!/usr/bin/perl -w

%main::tests = (
    'any' => {
        'comment' => 'lengauer_tarjan_dominator_tree algorithm tests',
        'data' => [ ],
        'tests' => [qw(
            doc-lengauerTarjanDominatorTree
            )],
        'documentation' => [qw(
            doc-lengauerTarjanDominatorTree
            )]
    },

);

1;

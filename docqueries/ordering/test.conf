#!/usr/bin/perl -w

%main::tests = (
    'any' => {
        'files' => [qw(
            cuthillMckeeOrdering.pg
            topologicalSort.pg
            kingOrdering.pg
            minDegreeOrdering.pg
            sloanOrdering.pg
            )]
    },

);

1;

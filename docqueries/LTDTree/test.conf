#!/usr/bin/perl -w

%main::tests = (
    'any' => {
        'comment' => 'LTDTree algorithm tests.',
        'data' => [ ],
        'tests' => [qw(
            doc-LTDTree
            )],
        'documentation' => [qw(
            doc-LTDTree
            )]
    },

);

1;

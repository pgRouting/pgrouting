#!/usr/bin/perl -w

%main::tests = (
    'any' => {
        'files' => [qw(
            issue900.pg
            createVertTab-any.pg
            createTopology.pg
            createVerticesTable.pg
            analyzeGraph.pg
            analyzeOneWay.pg
            nodeNetwork.pg
            extractVertices.pg
            degree.pg
            )],
    },
);

1;

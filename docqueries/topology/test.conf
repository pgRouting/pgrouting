#!/usr/bin/perl -w

%main::tests = (
    'any' => {
        'files' => [qw(
            issue900.pg
            createTopology.pg
            nodeNetwork.pg
            extractVertices.pg
            )],
    },
);

1;

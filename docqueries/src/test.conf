#!/usr/bin/perl -w

%main::tests = (
    'any' => {
        'files' => [qw(
            sampledata.pg
            withPoints-category.pg
            migration.pg
            concepts.pg
            )],
    },
);

1;

#!/usr/bin/perl -w

%main::tests = (
    'any' => {
        'comment' => '',
        'data' => [],
        'tests' => [qw(
            withPoints-category
            sampledata
            migration
            )],
        'documentation' => [qw(
            sampledata
            withPoints-category
            )],
    },
);

1;

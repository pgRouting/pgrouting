BEGIN;
BEGIN
SET client_min_messages TO NOTICE;
SET
/* -- q1 */
SELECT * FROM pgr_bandwidth(
'SELECT id, source, target, cost, reverse_cost
FROM edges'
);
 bandwidth
-----------
        13
(1 row)

/* -- q2 */
ROLLBACK;
ROLLBACK

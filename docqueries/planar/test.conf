#!/usr/bin/perl -w

%main::tests = (
    'any' => {
        'comment' => 'boyerMyrvold planarity algorithm tests.',
        'data' => [ ],
        'tests' => [qw(
            doc-pgr_boyerMyrvold
            )],
        'documentation' => [qw(
            doc-pgr_boyerMyrvold
            )]
    },

);

1;

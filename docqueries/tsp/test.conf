#!/usr/bin/perl -w

%main::tests = (
    'any' => {
        'comment' => 'TSP tests',
        'data' => [],
        'tests' => [qw(
            doc-pgr_TSPeuclidean
            )],
        'linux' => [qw(
            doc-pgr_TSP
            )],
        'macos' => [qw(
            doc-pgr_TSP
            )],
        # windows has a reversed path which is OK
        'windows' => [qw(
            win-pgr_TSP
            )],
        'documentation' => [qw(
            doc-pgr_TSP
            doc-pgr_TSPeuclidean
            )]
    },
);

1;

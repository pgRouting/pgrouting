#!/usr/bin/perl -w

%main::tests = (
    'any' => {
        'comment' => 'Dijkstra test for any versions.',
        'data' => [ ],
        'tests' => [qw(
            doc-MY_FUNCTION_NAME
            )],
        'documentation' => [qw(
            doc-MY_FUNCTION_NAME
            )]
    },

);

1;
